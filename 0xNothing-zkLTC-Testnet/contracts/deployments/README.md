
# Deployment Manifests

`DeployTestnet.s.sol` writes the most recent deployment addresses to
`latest.json`. Generated manifests are ignored until a reviewed deployment is
explicitly approved for source control.
