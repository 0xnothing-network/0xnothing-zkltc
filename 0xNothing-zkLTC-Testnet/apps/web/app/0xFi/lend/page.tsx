import { LendingWorkspace } from "@fi/components/LendingWorkspace";
import { PageHeading } from "@fi/components/UiStates";

export default function LendPage() {
  return <div className="fi-page"><PageHeading title="LEND" /><LendingWorkspace /></div>;
}
